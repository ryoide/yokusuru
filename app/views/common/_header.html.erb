<nav class="navbar navbar-default">
  <div class="container-fluid">
    <!-- Brand and toggle get grouped for better mobile display -->
    <div class="navbar-header">
      <%= link_to (image_tag "Y.png", :class => "navbar-brand"), root_path %>
    </div>
      <ul class="nav navbar-nav navbar-right">
        <li><%= link_to "投稿する", new_problem_path %></li>
        <% if request.path_info != "/home" %>
        <li><a href="/home">提案中</a></li>
        <% end %>
        <% if request.path_info != "/home/doing" %>
        <li><a href="/home/doing">改善中</a></li>
        <% end %>
        <li class="dropdown">
          <a href="#" class="dropdown-toggle" data-toggle="dropdown" role="button" aria-expanded="false">
            <%= image_tag "#{current_user.avatar}", :width => "20px", :height => "20px", :class => "img-circle" %>
            <%= current_user.name %> <span class="caret"></span></a>
          <ul class="dropdown-menu" role="menu">
            <li><%= link_to "マイページ", user_path(current_user.id) %></li>
            <li><%= link_to "ランキング", ranking_path(current_user.team.id) %></li>
            <li><%= link_to "#{current_user.team.name}", team_path(current_user.team.id) %></li>
            <li><%= link_to "お問い合わせ", new_contact_path %></li>
            <li class="divider"></li>
            <li><%= link_to 'ログアウト', destroy_user_session_path, method: :delete %></li>
          </ul>
        </li>
      </ul>
    </div><!-- /.navbar-collapse -->
  </div><!-- /.container-fluid -->
</nav>