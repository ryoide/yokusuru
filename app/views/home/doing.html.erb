<%= render partial: 'common/header' %>

<h1>改善中</h1>

<% if current_user.team.problems.where(done: 1).exists? == false %>
	<div>
		<p>投稿がありません</p>
	<div>
<% end %>

<div>
    <%= render partial: 'problems/problem', collection: @problems %>
</div>